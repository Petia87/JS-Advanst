let input = [3,2]

let print = this.print || console.log;
let gets = this.gets || ((arr, index) => () => arr[index++])(input, 0);
let n= +gets();
let x=+gets()
let f

S = 1 + 1!/x + 2!/x2 + … + N!/xN
for (let i = 0; i <n; i++) {
   let next=+gets()
    f=
}
    